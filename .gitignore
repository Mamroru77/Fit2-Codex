# Build output
[Bb]in/
[Oo]bj/
artifacts/

# Android / Gradle build output. The Gradle wrapper itself is committed on purpose: the wrapper
# JAR, the two launcher scripts and gradle-wrapper.properties are the pinned build entry point.
**/build/
.gradle/
local.properties
*.apk
*.aab
*.hprof
captures/

# Test results
[Tt]est[Rr]esult*/
*.trx
*.coverage
*.coveragexml

# IDE / editor
.vs/
.vscode/
.idea/
*.user
*.suo
*.userosscache

# Local machine scratch
.tmp-setup/

# Local agent workspace data (memory, not project source)
.workbuddy-ai/

# Local execution ledger (agent scratch, not project source)
.superpowers/
