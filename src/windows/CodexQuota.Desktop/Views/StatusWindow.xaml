<Window x:Class="CodexQuota.Desktop.Views.StatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Codex Quota Bridge"
        Height="280"
        Width="440"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" FontWeight="SemiBold" Text="Phase" />
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding PhaseText}" />

        <TextBlock Grid.Row="1" Grid.Column="0" FontWeight="SemiBold" Text="Quota source" />
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SourceStatusText}" />

        <TextBlock Grid.Row="2" Grid.Column="0" FontWeight="SemiBold" Text="Last successful sync" />
        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding LastSyncText}" />

        <TextBlock Grid.Row="3" Grid.Column="0" FontWeight="SemiBold" Text="5-hour window" />
        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ShortWindowText}" />

        <TextBlock Grid.Row="4" Grid.Column="0" FontWeight="SemiBold" Text="Weekly window" />
        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding WeeklyWindowText}" />

        <TextBlock Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                   Margin="0,12,0,0"
                   Foreground="Gray"
                   TextWrapping="Wrap"
                   Text="Closing this window hides it. Use Exit in the tray icon to stop the Bridge and its Codex App Server." />
    </Grid>
</Window>
